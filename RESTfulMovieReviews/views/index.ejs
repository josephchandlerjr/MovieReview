<% include partials/header %>


<div class="container">
	<div class="row">
		<% for(movie of movies) {%>
			<div class="card" >
			  <img class="card-img-top index-item" src="<%= movie.image %>" alt="Card image cap">
			  <div class="card-body">
				<a class="card-text" href="/movies/<%= movie._id%>">Show Review.</a>
			  </div>
			</div>
		<%}%>
	</div>
</div>

<% include partials/footer %>